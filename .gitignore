# Reference images (not needed for the website)
UGO v1 images/

# Architecture planning doc (local reference only)
ugo-modern-architecture.md

# macOS
.DS_Store

# Dependencies
node_modules/

# Local API key config (never commit)
orbit/config.js
config.js
telemetry-config.js
gallery/edit-config.js

# Local planning notes (never commit)
memory/
